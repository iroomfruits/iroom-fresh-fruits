# IROOM HOME1 V60.8 설치 UX / 아이콘 보강 점검

## 이번 수정 목적
V60.7에서 브라우저가 `beforeinstallprompt`를 늦게 발생시키거나 놓친 경우 긴 수동 설치 안내가 먼저 보이던 문제와, Windows/바탕화면 바로가기에서 이룸 로고가 너무 작게 보이던 문제를 보완했다.

## 적용 내용
- `beforeinstallprompt`를 `<head>`에서 가장 먼저 포착해 설치 이벤트 유실 가능성을 낮춤.
- Chrome / Edge / Android에서 설치 버튼을 누르면 최대 약 4.2초 동안 설치 이벤트를 기다린 뒤 준비되는 즉시 시스템 설치창을 실행하도록 보강.
- 설치 이벤트가 이미 준비된 경우 버튼 한 번으로 즉시 시스템 설치창 실행.
- Android의 카카오/네이버/인스타 등 인앱 브라우저에서는 Chrome 열기 흐름 유지.
- iPhone/iPad는 플랫폼 제약상 웹사이트가 설치창을 강제로 띄울 수 없으므로 Safari의 `공유 → 홈 화면에 추가`를 2단계 안내로 단순화.
- 기존 3단계 이상 긴 Edge 안내를 간결한 1단계 안내로 축소.
- 설치 아이콘은 업로드한 이룸 로고에서 작은 화면에서도 식별되는 `이룸 원형 심볼`을 크게 사용하도록 재제작.
- 32 / 64 / 180 / 192 / 512 / maskable 512 아이콘 재생성.
- Windows/브라우저 바로가기용 다중 해상도 `favicon.ico` 추가.
- manifest 아이콘 경로를 절대 경로로 정리하고 PWA 메타데이터 갱신.
- 서비스워커 캐시를 `iroom-v60-8-smart-install-one-tap`으로 갱신.

## 기대 동작
### Chrome / Edge / Android
1. 사용자가 `한 번 눌러 이룸 앱 설치` 클릭.
2. 설치 프롬프트가 준비되어 있으면 즉시 시스템 설치창 표시.
3. 아직 준비되지 않았으면 버튼에 `설치창 준비 중…` 표시 후 이벤트를 기다려 자동 실행 시도.
4. 브라우저가 설치 프롬프트를 제공하지 않는 특수 상황에서만 최소 안내 표시.

### iPhone / iPad
Apple Safari의 웹앱 설치 정책상 사이트에서 설치 확인창을 강제로 띄울 수 없다. 따라서 가장 짧은 방식인 `공유(□↑) → 홈 화면에 추가 → 추가`를 안내한다.

## 아이콘
- PWA 설치: 이룸 원형 심볼이 크게 표시되는 192/512 아이콘 사용.
- iPhone 홈 화면: 180px apple-touch-icon 사용.
- Windows/일반 바로가기: favicon.ico와 32/64 PNG 사용.
- 기존 바로가기/설치 앱은 운영체제 아이콘 캐시 때문에 이전 아이콘이 남을 수 있으므로, 배포 후 기존 바로가기/앱 삭제 후 다시 설치하면 V60.8 아이콘을 확인하기 쉽다.

## 검사 결과
- `node --check server.js` 통과
- `node --check public/iroom_assets/iroom-v60-8.js` 통과
- `node --check public/sw.js` 통과
- manifest JSON 파싱 통과
- 아이콘 크기 확인: 32/64/180/192/512/maskable 512 정상
- `favicon.ico` 생성 확인
- index.html ID 중복 없음

## 제한 및 실제 기기 확인 필요
PWA 설치창은 최종적으로 브라우저/운영체제가 제공한다. 특히 iOS는 자동 설치 프롬프트 API가 없다. V60.8은 웹사이트에서 가능한 범위에서 설치 이벤트를 가장 먼저 잡고, 소비자의 클릭 횟수를 최소화한 버전이다. Render 배포 후 Windows Edge/Chrome, Android Chrome/Edge, iPhone Safari 실기기 확인을 권장한다.

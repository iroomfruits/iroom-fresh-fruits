# IROOM HOME1 V60.24 — 이지플 전환 패치

기준: V60.23 위에 적용

## 변경 내용
- 홈페이지1 가을 시즌의 `홍로사과`를 `이지플`로 교체.
- 홈페이지2 Today PICK의 `홍로사과`를 `이지플`로 교체.
- 관리자 기본 시즌 설정/상품 선택 목록도 `이지플`로 변경.
- 기존 DB에 저장된 `홍로사과` / `경북 홍사과` 상품명을 배포 시 `이지플`로 자동 전환.
- 저장된 홈페이지 설정(site_settings) 안의 `홍로사과` / `경북 홍사과` 표기도 `이지플`로 자동 전환.
- 과거 주문(order_items)의 상품명은 주문 당시 기록 보존을 위해 변경하지 않음.
- 중복 구상품 `red-apple`은 기존 commerce 상품 `v37-hongro-apple`이 존재할 때 비활성화하여 storefront 중복 노출을 방지.
- 가격/구성은 기존 홍로사과 기준(32,000원 / 3kg)을 그대로 유지. 실제 판매가/재고는 관리자에서 변경 가능.
- 홈페이지2 스틸라이프 사과 이미지는 기존 스틸라이프 자산을 유지하며 `이지플`로 표시.

## 적용
1. `IROOM_HOME1_V60_24_EAZYPPLE_GITHUB_PATCH.zip` 압축 해제
2. GitHub 저장소 루트에 그대로 덮어쓰기
3. Render 재배포
4. `/home1`, `/home2`, 관리자 상품 목록 확인

## 확인 포인트
- 홈페이지1 가을 Today/Premium에 `이지플` 표시
- 홈페이지2 Today PICK에 `이지플` 표시
- 관리자 상품에서 현재 판매 상품이 `이지플`로 표시
- 기존 주문 내역은 원래 주문 당시 상품명 유지
